<div class="table-responsive">
    <table class="table table-edit table-bordered" style="table-layout: fixed">
        <colgroup>
            <col style="width: 200px" />
            <col style="width: 90px" />
            <col style="width: 90px" />
            <col style="width: 90px" />
            <col style="width: 90px" />
            <col style="width: 90px" />
            <col style="width: 30px" />
        </colgroup>
        <thead>
            <tr class="info">
                <th>Name</th>
                <th>
                    <span class="hidden-xs">Very Simple</span>
                    <span class="visible-xs-block">V.Simp</span>
                </th>
                <th>Simple</th>
                <th>Medium</th>
                <th>Complex</th>
                <th>
                    <span class="hidden-xs">Very Complex</span>
                    <span class="visible-xs-block">V.Comp</span>
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <tr ng-repeat="factor in viewModel.project.Factors track by $index">
                <td>
                    <input type="text"
                           class="form-control"
                           ng-model="factor.Name"
                           ng-attr-id="{{ 'factor-name-edit-' + $index }}" 
                           title="Enter name of this factor"
                           autocomplete="off" />
                </td>
                <td>
                    <input type="number"
                           class="form-control"
                           title="Enter hours for a very simple task"
                           ng-model="factor.VerySimple"
                           ng-attr-id="{{ 'factor-verySimple-edit-' + $index }}" 
                           autocomplete="off" />
                </td>
                <td>
                    <input type="number"
                           class="form-control"
                           title="Enter hours for a simple task"
                           ng-model="factor.Simple"
                           ng-attr-id="{{ 'factor-simple-edit-' + $index }}" 
                           autocomplete="off" />
                </td>
                <td>
                    <input type="number"
                           class="form-control"
                           title="Enter hours for a medium task"
                           ng-model="factor.Medium"
                           ng-attr-id="{{ 'factor-medium-edit-' + $index }}" 
                           autocomplete="off" />
                </td>
                <td>
                    <input type="number"
                           class="form-control"
                           title="Enter hours for a complex task"
                           ng-model="factor.Complex"
                           ng-attr-id="{{ 'factor-complex-edit-' + $index }}" 
                           autocomplete="off" />
                </td>
                <td>
                    <input type="number"
                           class="form-control"
                           title="Enter hours for a very complex task"
                           ng-model="factor.VeryComplex"
                           ng-attr-id="{{ 'factor-veryComplex-edit-' + $index }}" 
                           autocomplete="off" />
                </td>
                <td class="active">
                    <div class="btn-group pull-right">
                        <a class="btn btn-link btn-sm"
                           ng-click="viewModel.removeFactor(factor)"
                           ng-attr-id="{{ 'factor-remove-link-' + $index }}" 
                           title="Delete this factor">
                            <i class="glyphicon glyphicon-remove"></i>
                        </a>
                    </div>
                </td>
            </tr>
        </tbody>
        <tfoot>
            <tr>
                <th colspan="7">
                    <div class="btn-group">
                        <a id="add-factor-button"
                           class="btn btn-link btn-sm"
                           ng-click="viewModel.addFactor()">
                            <i class="glyphicon glyphicon-plus"></i>
                            Add Factor
                        </a>
                        <a id="add-template-button"
                           class="btn btn-link btn-sm"
                           ng-click="viewModel.addTemplate()">
                            <i class="glyphicon glyphicon-plus"></i>
                            Add Template
                        </a>
                        <a id="reorder-factor-button"
                           class="btn btn-link btn-sm"
                           ng-click="viewModel.reorderFactors()"
                           ng-show="viewModel.project.Factors.length > 1">
                            <i class="fa fa-unsorted"></i>
                            Reorder Factors
                        </a>
                    </div>

                </th>
            </tr>
        </tfoot>
    </table>
</div>

<div class="bs-callout bs-callout-primary"
     ng-hide="$localStorage.hideFactorCallout && viewModel.project.Factors.length > 0">
    <button type="button" class="close" aria-label="Close"
            title="Hide help box"
            ng-click="$localStorage.hideFactorCallout = true">
        <span aria-hidden="true">&times;</span>
    </button>
    <span class="fa fa-info-circle fa-2x pull-left text-primary"></span>
    Factors are a type of task with hours associated with the complexity of that task. Factors allow the estimator
    to state hours based on a specific type of task, regardless of the project.
</div>
