@{
    ViewBag.Title = "Create Estimate";
}

<div class="container" ng-controller="estimateEditController">
    <ol class="breadcrumb">
        <li><a href="@Url.Action("Index", "Home")">Home</a></li>
        <li><a href="@Url.Action("Index")">Estimates</a></li>
        <li class="active">Create</li>
    </ol>
    @Html.Partial("_Estimate")
</div>